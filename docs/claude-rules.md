# Claude Rules

* Be concise
* Return code only unless explanation requested
* Build modular components
* Use reusable architecture
* Keep code production-ready
* Avoid unnecessary abstractions
* Optimize for MVP speed
* Use Next.js + Tailwind + Supabase
* Prefer server actions where useful
* Keep components clean and reusable
* Avoid giant files
* Use TypeScript
* Follow modern React practices
* Mobile responsive by default
* Dark mode first
* Avoid overengineering
